<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#1F2654</item> <!-- Your Galileo Blue -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash</item>
        <item name="postSplashScreenTheme">@style/Theme.GalileoProject</item> <!-- Your main theme -->
    </style>
    
    <!-- 
      AppCompatActivity requires an AppCompat theme (or descendant). 
      Using Theme.AppCompat.DayNight.NoActionBar as the bridge for Compose.
    -->
    <style name="Theme.GalileoProject" parent="Theme.AppCompat.DayNight.NoActionBar" />
</resources>
